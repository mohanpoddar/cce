---
- hosts: all,localhost
  # connection: local
  gather_facts: true

# Playbook pre tasks
  pre_tasks:
    - name: Display Hostname
      debug: var=ansible_facts['hostname']

    - name: Print tmp_dir variable
      debug: var=tmp_dir

# # Add playbook roles
  roles:
  - home_ubuntu_setup
  - onedrive_install
  - samba